# Application icon

Place the final multi-resolution Windows icon at:

`EchoesOfAzeroth.ServerManager/Assets/AppIcon.ico`

The file must be named exactly `AppIcon.ico`. Once present, the project automatically embeds it as the executable icon and loads it as the WPF window/taskbar icon. The release can still be built without the file; no placeholder icon is generated.
